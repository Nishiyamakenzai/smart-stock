-- ────────────────────────────────────────────────────────
-- 新しいメンバーをSQLで直接追加したい場合の例（任意）
-- 通常は「タスク管理 > 設定 > メンバー管理」画面から追加する方が簡単です。
-- ────────────────────────────────────────────────────────

insert into members (name, role, color, display_order, is_active)
values (
  '白鳥 龍希',      -- 氏名（評価制度でもこの名前がそのまま使われます）
  '職人',           -- 役職（自由入力）
  '#0891B2',        -- バッジの色（お好みで変更可）
  (select coalesce(max(display_order), 0) + 1 from members),
  true
);
